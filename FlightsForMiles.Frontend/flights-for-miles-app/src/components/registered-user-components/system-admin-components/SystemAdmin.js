import React from 'react'
import Header from './Header'

function SystemAdmin() {
    return (
        <div>
            <Header />
            {/* <Switch>
                ovde ce ici rute za svaku od operacija main admin-a
                <Route exact path="/system/:username?" component={ConfirmRegYes} />
            </Switch>*/}
        </div>
    )
}

export default SystemAdmin
